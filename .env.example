PORT=3000
NODE_ENV=development
LOG_LEVEL=info
API_KEY=__FROM_SECRETS_MANAGER__
# SECURITY: do not store production secrets in plaintext .env files.
# Use a secrets manager (e.g., AWS Secrets Manager, GCP Secret Manager, Vault)
# and inject values at runtime through your deployment platform.
DATABASE_URL=__FROM_SECRETS_MANAGER__
REDIS_URL=__FROM_SECRETS_MANAGER__

# Local example (docker compose default)
# DATABASE_URL=postgres://postgres:postgres@localhost:5433/omniscribe
